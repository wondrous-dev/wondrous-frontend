import Grid from '@mui/material/Grid';

const PodItem = () => (
  <Grid sx={{ display: 'flex', alignItems: 'center', gap: '65px' }}>
    <Grid sx={{ display: 'flex', alignItems: 'center', gap: '16px' }}>yo</Grid>
    <Grid sx={{ display: 'flex', alignItems: 'center', gap: '16px' }}>yo</Grid>
  </Grid>
);

export default PodItem;
