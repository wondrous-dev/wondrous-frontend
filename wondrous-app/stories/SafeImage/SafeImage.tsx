import React from 'react';

export const SafeImage = () => {
  return (
    <div>
      SafeImage Component - is a component with which, when an image is loaded, it is displayed (as a preview).
      <br />
      How it works: a link to the image is passed through the src prop.
    </div>
  );
};
