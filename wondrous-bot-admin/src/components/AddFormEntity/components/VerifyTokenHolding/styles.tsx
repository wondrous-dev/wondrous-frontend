import { Typography } from "@mui/material";
import styled from "styled-components";

export const TokenImage = styled.img`
  margin-right: 6px;
`;
export const TokenText = styled(Typography)`
  && {
    font-family: Poppins;
    color: grey;
    font-size: 14px;
  }
`;
