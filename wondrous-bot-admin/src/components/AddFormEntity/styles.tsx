import { Grid } from '@mui/material';
import styled from 'styled-components';

export const Header = styled(Grid)`
  && {
    background: #f7f7f7;
    padding: 14px;
    width: 100%;
    border-radius: 16px;
  }
`;
